Place your trained ONNX model here after running:

  cd model
  pip install -r requirements.txt
  python train.py
  python export_onnx.py

The export script will copy the file here automatically as:
  pill_detection.onnx
